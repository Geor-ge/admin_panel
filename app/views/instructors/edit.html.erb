<div class="row justify-content-center">
  <div class="col-3">
    <h2><%= "#{@instructor.f_name} #{@instructor.l_name}" %></h2>
    <%= form_for(@instructor) do |f| %>
    <%= render partial: 'form', locals: {f: f} %>
    <p class='button'><%= f.submit 'Edit Instructor' %></p>
    <% end %>
  </div>
</div>
