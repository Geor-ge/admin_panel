<div class="row justify-content-center">
  <div class="col-3">
    <h3><%= "#{@instructor.f_name} #{@instructor.l_name}" %></h3>
    <%= form_for(@instructor) do |f| %>
    <%= render partial: 'form', locals: {f: f} %>
    <p><%= f.submit 'Edit Instructor' %></p>
    <% end %>
  </div>
</div>
