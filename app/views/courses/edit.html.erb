<div class="row justify-content-center">
  <div class="col-3">
    <h2><%= @course.name %></h2>
    <%= form_for(@course) do |f| %>
    <%= render partial: 'form', locals: {f: f} %>
    <p class='button'><%= f.submit 'Edit Course' %></p>
    <% end %>
  </div>
</div>
