<div class="row justify-content-center">
  <div class="col-4">
    <div class="card text-center">
      <div class="card-body">
        <h4 class="card-title"><%= @course.name %> </h4>
      </div>
        <p>Total Hours: <%= @course.total_hours %></p>
      <div class="card-body">
        <%= link_to 'Edit Course', edit_course_path, data: {turbolinks: 'false'}, class: "card-link" %>
      </div>
    </div>
  </div>
</div>
