
<div class="row justify-content-center">
  <div class="col-3">
    <h2><%= "#{@student.f_name} #{@student.l_name}" %></h2>
    <%= form_for @student do |f| %>
    <%= render partial: 'form', locals: {f: f} %>
    <p class='button'><%= f.submit 'Edit Student' %></p>
    <% end %>
  </div>
</div>
