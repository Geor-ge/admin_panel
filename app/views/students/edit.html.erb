
<div class="row justify-content-center">
  <div class="col-3">
    <h3><%= "#{@student.f_name} #{@student.l_name}" %></h3>
    <%= form_for @student do |f| %>
    <%= render partial: 'form', locals: {f: f} %>
    <p><%= f.submit 'Edit Student' %></p>
    <% end %>
  </div>
</div>
